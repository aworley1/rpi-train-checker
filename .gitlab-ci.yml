stages:
  - build

build:
  stage: build
  image: azul/zulu-openjdk-alpine:12
  script:
    - ./gradlew --no-daemon clean test build jib
  artifacts:
    when:
      on_failure
    paths:
      - build/reports/
  only:
    - master